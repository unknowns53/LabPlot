# GPC解析・図作成アプリ 開発手順書

## 1. 目的

高分子のGPC測定データを読み込み、解析・可視化・出力を行うWindowsデスクトップアプリを作成する。

研究室内での使用を想定し、プログラミングに不慣れなユーザーでも扱えるGUIアプリとする。
Excelで毎回手作業する工程を減らし、測定データの読み込み、グラフ作成、ピーク範囲指定、解析結果出力を簡単に行えるようにする。

---

## 2. 開発環境

以下を前提とする。

```text
OS: Windows 10 / Windows 11
言語: C#
フレームワーク: .NET 10
GUI: WPF
IDE: Visual Studio Community
```

使用予定ライブラリは以下。

```text
ScottPlot        グラフ描画
CsvHelper        CSV読み込み
ClosedXML        Excel出力
MathNet.Numerics 数値計算
```

ただし、最初から全部を入れなくてよい。
まずはCSV読み込みとグラフ表示のMVPを作る。

---

## 3. 基本方針

このアプリでは、**GUI部分と解析ロジックを分離する**。

WPF画面側に解析処理を直接書きすぎないこと。
CSV読み込み、データ変換、ベースライン補正、ピーク範囲処理、分子量計算などは `Core` プロジェクトに置く。

推奨構成は以下。

```text
GpcAnalyzer/
  GpcAnalyzer.App/        WPFアプリ本体
  GpcAnalyzer.Core/       解析ロジック
  GpcAnalyzer.Tests/      単体テスト
```

`GpcAnalyzer.App` は画面表示とユーザー操作を担当する。
`GpcAnalyzer.Core` は測定データの処理を担当する。
`GpcAnalyzer.Tests` は解析処理のテストを担当する。

---

## 4. 最初に作るMVP

最初の目標は、以下の機能だけに絞る。

```text
1. CSVファイルを選択できる
2. CSVから時間または保持時間と信号強度を読み込める
3. 読み込んだデータを画面にグラフ表示できる
4. グラフをPNGとして保存できる
```

この時点では、分子量計算、ピーク解析、ベースライン補正、Excel出力はまだ実装しない。

まず「読み込む → 表示する → 保存する」だけを確実に動かす。

---

## 5. 最初の画面構成

初期画面には、以下の要素を置く。

```text
[CSVを開く] ボタン
[グラフを保存] ボタン
読み込んだファイルパスの表示欄
グラフ表示エリア
ステータスメッセージ表示欄
```

画面イメージは以下。

```text
--------------------------------------------------
GPC Analyzer

[CSVを開く]  [グラフを保存]

ファイル:
C:\Users\...\sample.csv

--------------------------------------------------
|                                                |
|                 GPC chromatogram              |
|                                                |
--------------------------------------------------

ステータス: CSVを読み込みました。
--------------------------------------------------
```

---

## 6. CSV読み込み仕様

最初は単純なCSVを想定する。

例：

```csv
Time,Signal
0.00,0.012
0.01,0.013
0.02,0.015
...
```

または、

```csv
RetentionTime,Intensity
10.0,123
10.1,128
10.2,140
...
```

初期実装では、以下のような方針にする。

```text
- 1行目をヘッダーとして扱う
- 1列目をX軸データとして扱う
- 2列目をY軸データとして扱う
- 数値変換できない行はスキップする
- 読み込み失敗時はエラーメッセージを表示する
```

将来的には、ユーザーが列を選択できるようにする。

---

## 7. Core側の最小クラス設計

`GpcAnalyzer.Core` に以下のようなクラスを作る。

```csharp
public sealed class GpcDataPoint
{
    public double X { get; init; }
    public double Y { get; init; }
}
```

```csharp
public sealed class GpcDataset
{
    public string? SourceFilePath { get; init; }
    public IReadOnlyList<GpcDataPoint> Points { get; init; } = Array.Empty<GpcDataPoint>();
}
```

```csharp
public interface IGpcDataReader
{
    GpcDataset Read(string filePath);
}
```

```csharp
public sealed class CsvGpcDataReader : IGpcDataReader
{
    public GpcDataset Read(string filePath)
    {
        // CSVを読み込み、1列目をX、2列目をYとして返す
    }
}
```

このようにしておくと、あとでCSV以外の形式にも対応しやすい。

---

## 8. App側の最小処理

`GpcAnalyzer.App` 側では、以下を実装する。

```text
- OpenFileDialogでCSVを選択する
- CsvGpcDataReaderでデータを読み込む
- ScottPlotでグラフ表示する
- SaveFileDialogでPNG保存する
```

ボタンの処理に全ロジックを詰め込まないこと。
ボタンの処理は、Coreのクラスを呼び出すだけに近づける。

悪い例：

```csharp
private void OpenCsvButton_Click(...)
{
    // ファイル選択
    // CSV読み込み
    // 数値変換
    // 解析
    // グラフ描画
    // エラー処理
    // すべてここに書く
}
```

良い例：

```csharp
private void OpenCsvButton_Click(...)
{
    var filePath = SelectCsvFile();
    var dataset = _reader.Read(filePath);
    PlotDataset(dataset);
}
```

---

## 9. 実装ステップ

### Step 1: ソリューション作成

Visual StudioまたはCLIで以下の構成を作る。

```text
GpcAnalyzer
  GpcAnalyzer.App
  GpcAnalyzer.Core
  GpcAnalyzer.Tests
```

CLIで作る場合の例：

```powershell
dotnet new sln -n GpcAnalyzer

dotnet new wpf -n GpcAnalyzer.App
dotnet new classlib -n GpcAnalyzer.Core
dotnet new xunit -n GpcAnalyzer.Tests

dotnet sln add GpcAnalyzer.App/GpcAnalyzer.App.csproj
dotnet sln add GpcAnalyzer.Core/GpcAnalyzer.Core.csproj
dotnet sln add GpcAnalyzer.Tests/GpcAnalyzer.Tests.csproj

dotnet add GpcAnalyzer.App/GpcAnalyzer.App.csproj reference GpcAnalyzer.Core/GpcAnalyzer.Core.csproj
dotnet add GpcAnalyzer.Tests/GpcAnalyzer.Tests.csproj reference GpcAnalyzer.Core/GpcAnalyzer.Core.csproj
```

### Step 2: Coreにデータ構造を作る

`GpcDataPoint`、`GpcDataset`、`IGpcDataReader`、`CsvGpcDataReader` を作る。

### Step 3: CSV読み込みテストを書く

`GpcAnalyzer.Tests` に、簡単なCSVを読み込ませるテストを作る。

確認項目：

```text
- データ点数が正しい
- X値が正しく読める
- Y値が正しく読める
- 不正な行をスキップできる
- 空ファイルで例外または適切なエラーになる
```

### Step 4: WPF画面を作る

`MainWindow.xaml` に以下を配置する。

```text
CSVを開くボタン
グラフ保存ボタン
ファイルパス表示欄
ScottPlot表示エリア
ステータスメッセージ欄
```

### Step 5: CSVを開く処理を実装する

OpenFileDialogでCSVを選択し、CoreのReaderで読み込む。

### Step 6: グラフ表示を実装する

ScottPlotを使い、X-Yデータを折れ線グラフとして表示する。

### Step 7: PNG保存を実装する

SaveFileDialogで保存先を選び、表示中のグラフをPNG出力する。

### Step 8: READMEを作る

使い方を簡単に書く。

```text
1. CSVを開く
2. グラフを確認する
3. 必要ならPNGで保存する
```

---

## 10. 将来的に追加する機能

MVP完成後、以下を段階的に追加する。

### 第2段階：解析補助

```text
- グラフのX軸・Y軸範囲指定
- 軸ラベル変更
- タイトル変更
```

### 第3段階：GPC解析

```text
- 較正曲線の読み込み
- 保持時間から分子量への変換
- Mn計算
- Mw計算
- PDI = Mw / Mn の計算
- ピークトップ分子量の算出
- 重ね書き
- 書式設定
```

### 第4段階：出力機能

```text
- 解析結果をCSV出力
- 解析結果をExcel出力
- グラフ画像出力
- 解析条件の保存
```

### 第5段階：研究室配布向け整備

```text
- exe化
- 設定ファイル対応
- サンプルデータ同梱
- README整備
- エラー時のメッセージ改善
```

---

## 11. 注意点

このアプリでは、最初から完璧なGPC解析ソフトを目指さない。

最初に作るべきものは、あくまで以下。

```text
CSVを読む
グラフを描く
画像として保存する
```

この3つが安定してから、ピーク解析や分子量計算を追加する。

また、GUIに処理を詰め込みすぎないこと。
後から機能を追加しやすいように、解析ロジックは必ず `GpcAnalyzer.Core` に置く。

---

# Codexに最初に投げる指示文

以下をそのままCodexに貼ってください。

```text
C# / WPF / .NET 10 で、研究室用のGPC解析・図作成アプリを作りたいです。

目的は、高分子のGPC測定データCSVを読み込み、グラフ表示し、PNGとして保存できるWindowsデスクトップアプリを作ることです。

まずはMVPとして、以下だけ実装してください。

1. CSVファイルを選択できる
2. CSVの1列目をX軸、2列目をY軸として読み込む
3. 読み込んだデータをWPF画面上に折れ線グラフとして表示する
4. 表示中のグラフをPNGとして保存できる
5. 読み込みエラー時には画面上にエラーメッセージを表示する

技術構成は以下です。

- .NET 10
- C#
- WPF
- ScottPlot for WPF
- CsvHelper
- xUnit

プロジェクト構成は以下にしてください。

GpcAnalyzer/
  GpcAnalyzer.App/
  GpcAnalyzer.Core/
  GpcAnalyzer.Tests/

重要な方針として、CSV読み込みやデータ処理などの解析ロジックは GpcAnalyzer.Core に置き、WPF画面側にはできるだけ処理を書き込まないでください。

GpcAnalyzer.Core には、少なくとも以下のクラスまたは同等の構造を作ってください。

- GpcDataPoint
- GpcDataset
- IGpcDataReader
- CsvGpcDataReader

CSV仕様は、初期実装では以下のようにしてください。

- 1行目はヘッダーとして扱う
- 1列目をX軸データとして扱う
- 2列目をY軸データとして扱う
- 数値変換できない行はスキップする
- 読み込める有効データがない場合は例外またはエラーとして扱う

WPF画面には以下を配置してください。

- 「CSVを開く」ボタン
- 「グラフを保存」ボタン
- 読み込んだファイルパスの表示欄
- ScottPlotによるグラフ表示エリア
- ステータスメッセージ表示欄

また、GpcAnalyzer.Tests に CsvGpcDataReader の単体テストを追加してください。

まずは、上記MVPがビルド・実行できる状態にしてください。
```

---

## Codexに一度で全部やらせない方がいいです

最初の依頼は上のMVPまでにしてください。
いきなり「ピーク検出、ベースライン補正、分子量計算、Excel出力まで全部」と投げると、かなりの確率で設計が濁ります。

おすすめの順番はこれです。

```text
第1依頼: CSV読み込み + グラフ表示 + PNG保存
第2依頼: 画面レイアウト整理
第3依頼: ピーク範囲指定
第4依頼: ベースライン補正
第5依頼: 較正曲線と分子量計算
第6依頼: Excel出力
第7依頼: 配布用ビルド
```

まずは **第1依頼だけ**をCodexに渡すのがよいです。
ここで骨格を綺麗に作っておくと、後の機能追加がかなり楽になります。
